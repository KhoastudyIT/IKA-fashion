# Server
PORT=4000
NODE_ENV=development

# JWT
JWT_SECRET=ika-fashion-dev-secret-change-in-prod
JWT_EXPIRES_IN=7d

# CORS — origin của Next.js frontend
CORS_ORIGIN=http://localhost:3000

# Tài khoản admin mặc định (seed lúc khởi động)
ADMIN_EMAIL=admin@ika.vn
ADMIN_PASSWORD=admin123

# API docs (Scalar)
OPENAPI_ENABLED=true
